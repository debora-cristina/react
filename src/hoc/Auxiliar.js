
const aux = (props)=> props.children;

export default aux;

// const aux = (props)=> props.children;

// // export default aux;
// const aux = (props)=> props.children;

// // export default aux;
// const aux = (props)=> props.children;

// // export default aux;
// const aux = (props)=> props.children;

// // export default aux;